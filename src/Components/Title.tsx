import React from "react";

interface Props {

};

export const Title: React.FC<Props> = () => {
    return (
        <header>
            <h1 id="Title">The Modern Registry</h1>
        </header>
    );
};